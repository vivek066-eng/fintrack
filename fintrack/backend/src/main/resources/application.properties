spring.application.name=fintrack-backend
server.port=8080

# ---- MySQL Configuration ----
spring.datasource.url=jdbc:mysql://localhost:3306/fintrack_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=9696
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---- JPA / Hibernate ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ---- JWT Configuration ----
jwt.secret=fintrackSuperSecretKeyForJWTTokenGenerationChangeThisInProduction123456
jwt.expiration.ms=86400000

# ---- CORS ----
app.cors.allowed-origins=http://localhost:4200
allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
